/*async function LoginFlow()
{
    try
    {
        const login =
        await Login();

        const profile =
        await GetProfile();

        const orders =
        await GetOrders();

        console.log(profile);
        console.log(orders);
    }
    catch(error)
    {
        console.log(error);
    }
}
    */
